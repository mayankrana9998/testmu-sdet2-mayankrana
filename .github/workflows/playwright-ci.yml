name: Playwright Quality Gate

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  api-tests:
    name: api
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run API suite
        run: npm run test:api

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-api
          path: |
            playwright-report/
            test-results/
          if-no-files-found: warn

      - name: Failure notification (GitHub summary)
        if: failure()
        run: |
          echo "## ❌ Playwright API failure detected" >> $GITHUB_STEP_SUMMARY
          echo "Suite: api" >> $GITHUB_STEP_SUMMARY
          echo "Download uploaded artifacts from this run for debugging." >> $GITHUB_STEP_SUMMARY

  ui-integration-tests:
    name: ${{ matrix.suite }} | ${{ matrix.browser }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        suite: [ui, integration]
        browser: [chromium, firefox]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps ${{ matrix.browser }}

      - name: Run UI suite
        if: matrix.suite == 'ui'
        run: npx playwright test tests/ui --project=${{ matrix.browser }}

      - name: Run integration suite
        if: matrix.suite == 'integration'
        run: npx playwright test tests/integration --project=${{ matrix.browser }}

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-${{ matrix.suite }}-${{ matrix.browser }}
          path: |
            playwright-report/
            test-results/
          if-no-files-found: warn

      - name: Failure notification (GitHub summary)
        if: failure()
        run: |
          echo "## ❌ Playwright failure detected" >> $GITHUB_STEP_SUMMARY
          echo "Suite: ${{ matrix.suite }}" >> $GITHUB_STEP_SUMMARY
          echo "Browser: ${{ matrix.browser }}" >> $GITHUB_STEP_SUMMARY
          echo "Download uploaded artifacts from this run for screenshots/video/trace." >> $GITHUB_STEP_SUMMARY
